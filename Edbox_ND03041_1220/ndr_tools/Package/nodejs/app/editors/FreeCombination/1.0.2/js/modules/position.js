var point = function(top,left,width,height){
    this.top = top;
    this.left = left;
    this.width = width;
    this.height = height;
}
//旋转后的实际坐标
point.prototype.rotate = function(rotate){
    var center = getCenter();
    var r = Math.sqrt(top-center.x,left-center.y);
    return {
        x: 1,
        y: 2
    }
}

center = x